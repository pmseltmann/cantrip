# Cantrip — never commit secrets
config/settings.json

# Runtime state (contains real Discord IDs)
config/bots.json

# Worker memory files (large, per-session)
projects/*/.memory/

# Manager memory files
docs/memory/manager/*.md

# Runtime artifacts
.caffeinate.pid
.bots-json.lock

# Generated artifacts
cantrip-architecture.html

# macOS
.DS_Store

# Node (if projects use it)
node_modules/
